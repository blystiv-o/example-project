import { AppShell } from '@/components/app-shell';

export default function ProfilePage() {
  return (
    <AppShell
      title="Профіль"
      description="Тут відображатимуться дані користувача й особистого простору."
    />
  );
}
